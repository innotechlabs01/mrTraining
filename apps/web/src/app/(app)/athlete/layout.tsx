'use client'

import { AthleteLayout } from '@/features/athlete'

export default function AthleteRouteLayout({ children }: { children: React.ReactNode }) {
  return <AthleteLayout>{children}</AthleteLayout>
}
