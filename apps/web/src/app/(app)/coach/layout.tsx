import CoachLayout from '@/features/coach/components/layout/CoachLayout'

export default function CoachRouteLayout({ children }: { children: React.ReactNode }) {
  return <CoachLayout>{children}</CoachLayout>
}
