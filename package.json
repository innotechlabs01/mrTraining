{
  "name": "mr-training",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @mrtraining/web dev",
    "dev:mobile": "cd apps/apps/mobile && npm start",
    "build": "pnpm --filter @mrtraining/web build",
    "build:mobile": "cd apps/apps/mobile && eas build --profile development --platform ios",
    "lint": "pnpm --filter @mrtraining/web lint",
    "test": "node_modules/.bin/jest"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^30.4.2",
    "jest-environment-jsdom": "^30.4.1",
    "ts-jest": "^29.4.11"
  }
}
